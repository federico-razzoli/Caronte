// Caronte configuration file

"use strict";

UTILE.SWDefaultOptions = {
	showInfo            : true,        // show info in menu
	light               : false,       // light version
	noExec              : false,       // load Caronte but dont load Application
	winTitle            : "Caronte",   // title in browser's window
	defaultTheme        : "classic",   // theme selected by default (id)
	defaultDictionary   : "it",        // default dict for Applications
	defaultLang         : "it"         // default UI lang
};

// all themes avaible for users / Applications
UTILE.allThemes = {
	classic	: {
		label           : "Classic",
	},
	black : {
		label           : "Back in blacK"
	}
};

